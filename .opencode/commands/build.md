---
description: Windows 原生构建 game-host
---
在仓库根目录运行 `ops build game-host`，构建 Windows 原生 `game-host`。可选参数 `run` 时改为运行 `ops run game-host`（同步镜像、重建并启动渲染）。完成后如实说明 Windows 原生运行是否已实际验收；没有 Windows 环境时明确该验证未运行。报告结果。
