# 地图脚本约定

本目录只放 Python 脚本。不要为 Windows 单独维护 PowerShell 或批处理版本。

- 生成器必须可由仓库根目录下的 `python scripts/maps/<script>.py` 运行。
- 脚本只读取已提交的素材和地图定义；生成物必须写到其明确的输出目录。
- 生成地图前先定义区域构图和可走空间。脚本负责重复劳动、边界校验和格式输出，不替代地图设计。
- 预览或校验脚本应使用 Python；若需要第三方库，要在脚本和相邻文档中写明依赖。
